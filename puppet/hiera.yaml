---
:backends:
    - yaml

:yaml:
    :datadir: /vagrant/puppet/hieradata
:merge_behavior: deeper
:hierarchy:
    - "infrastructure/%{::role}_%{infrastructure}"
    - "infrastructure/%{::infrasructure}"
    - common

